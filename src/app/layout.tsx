import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Gamika Jayawardana | AI ML Engineer & Full Stack Developer",
  description:
    "Portfolio of Gamika Jayawardana — AI ML Engineer & Full Stack Developer. Bridging intelligent algorithms with robust full-stack solutions.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.variable}>{children}</body>
    </html>
  );
}
